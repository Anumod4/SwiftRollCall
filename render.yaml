services:
  - type: web
    name: swiftrollcall
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
      - key: PORT
        value: 3000

  - type: static
    name: swiftrollcall-parent
    env: static
    buildCommand: cd mobile && npm install && npm run build
    publishDir: mobile/dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html

